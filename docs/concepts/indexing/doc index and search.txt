Most Relevant Document for Search:
1. Know of document existence - For example using web pages crawler
    specify document to index

2. index the document for lookup - Inverted index
Elasticsearch create the inverted index "behind the scene"

3. Know how rlevant the cocument is - Scoring
Applies a scoring algorithm for each document for each term


4. retrieve ranked by relevance - search
Retrieves documents using a Query DSL

indexing:
Tokenize text into words:
* split words
* lower  case
* remove punctuation


